import { ZipFilePreviewLogic } from "./index";

export function connect($img: HTMLDivElement, store: ZipFilePreviewLogic) {
  // const io = new IntersectionObserver(
  //   (entries) => {
  //     entries.forEach((entry) => {
  //       if (entry.isIntersecting) {
  //         store.ready();
  //         io.unobserve($img);
  //       }
  //     });
  //   },
  //   { threshold: 0.01 }
  // );
  // io.observe($img);
}
